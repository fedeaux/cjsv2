@CJSV:
  embedded_coffee: () ->
    _outstream=""

    if message
    _oustream += "  <p class=\"warning\">#{message}"
    for queijo in salada
    _oustream += "  </p>"
    _oustream += "  <span>#{queijo}"
    if simple_nesting
    unless double_nesting
    _oustream += "    <img src=\"neymar.png\"/>"
    if omg
    for queijo in salada
    _oustream += "      <span>#{queijo}"
    _oustream += "      </span>"
    _oustream += "  </span>"
