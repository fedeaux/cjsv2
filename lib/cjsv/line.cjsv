#mas
  #sera
  #que
    .foi
      - if foi
        .algreia
      - else
        .tem
          .que
        .fazer.quebrar
          .mesmo
  - if oi
    #oi
      %span
      - while sera
        - if sera.sim
          #felicidade
        - a = b
        .qual-e
          + sera.que.partial()
          .q
